import {ResourcePath} from "resource-path";

/**
 * A {@link ResourcePath} that indicates the location of the {@link AntagonistPage}
 */
export const AntagonistPagePath: ResourcePath = "/antagonist";
