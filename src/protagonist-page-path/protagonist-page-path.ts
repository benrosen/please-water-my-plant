import {ResourcePath} from "resource-path";

/**
 * A {@link ResourcePath} that indicates the location of the {@link ProtagonistPage}
 */
export const ProtagonistPagePath: ResourcePath = "/protagonist";
